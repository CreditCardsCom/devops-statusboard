sudo: required
services:
  - docker

language: elixir
elixir:
  - 1.4.2
otp_release:
  - 19.2

before_deploy:
  - MIX_ENV=prod mix release

deploy:
  provider: script
  script: docker/publish.sh
  skip_cleanup: true
  on:
    tags: true
